doctype html
html(lang="jp")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どちらが食べたいですか？
    form(method="post", action=path)
      span 名前 🧑👩: 
      input(type="text", name="name")
      input(type="radio", name="favorite", value=firstItem)
      span #{firstItem} 
      input(type="radio", name="favorite", value=secondItem)
      span #{secondItem} <br>
    button(type="submit") 投稿 ✔ <br>
    br
    a(href="/logout") ログアウト ✖